services:
  db:
    image: postgres:15
    container_name: gcs_postgres
    environment:
      POSTGRES_DB: gcs_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

volumes:
  postgres_data:
